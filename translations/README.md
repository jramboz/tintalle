# Tintallë translations

This directory contains the application translation catalogues.

- `.ts` files are editable translation sources.
- `.qm` files are compiled catalogues loaded by Tintallë at runtime.

Translation sources are updated with `pyside6-lupdate` and compiled with
`pyside6-lrelease`.

English is the source language and does not require a translation catalogue.
